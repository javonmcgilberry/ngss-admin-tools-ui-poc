import styles from './user.module.scss';
const User = () => {
    return (
        <div className={styles.user}>
            <h1>J. Johnson</h1>
            <span>SIGN OUT</span>
        </div>
    );
};

export default User;
