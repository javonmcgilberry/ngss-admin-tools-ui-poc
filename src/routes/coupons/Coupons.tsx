import { Section, Title } from '../../components/common/Common';

const Coupons = () => {
    return (
        <div>
            <Title title="Coupons" />
            <Section title="Pricing Options">
                <div></div>
            </Section>
            <Section title="Component #1">
                <div></div>
            </Section>
        </div>
    );
};

export default Coupons;
